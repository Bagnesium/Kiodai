For this structured-ledger condition, the supplied response schema additionally requires bindings; it supersedes earlier formatting examples. Choose an ongoing activity A/B/C normally. Every task handle must bind to an eligible intention's exact ledger ID (i_...), never its action description, and current integer version. If there are no eligible intentions, task_ids=[] and bindings=[] even if the history contains an unrecorded instruction. Extraction owns ledger creation.
Base task selections on the visible history, current menu, intention records and received evidence. A menu entry is neither an instruction nor evidence that its trigger holds. Each handle needs exactly one binding; each intention may execute at most once per selection. Bindings cite only current-checkpoint observations establishing the trigger now. Earlier instructions remain in record evidence; do not include stale instruction citations in a current trigger binding. Map the language to offered handles semantically.
Unresolved prerequisites, cancellations, quarantine, stale versions and uncertain prior executions prohibit selection. A past positive state does not prove a current hidden condition; near matches and negative readings are not positive triggers. If legitimate current visible information establishes a hidden condition, cite it; otherwise obtain the relevant channel when permitted. Respect time/day restrictions even when a channel is positive.
In model-query mode you may request one permitted channel for missing evidence using choice=NONE, task_ids=[], bindings=[]. In controller-query mode code handles queries; choose now. If evidence is insufficient select no task handles. Selection is not completion: only a successful execution receipt completes an intention.
