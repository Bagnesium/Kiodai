# Kiodai v2

**11 September 2026: comparison authorized, blocked before inference.** The frozen preflight passed, but the OpenRouter key had $4.92988004 remaining and the account had $9.92988004 credit; both are below the required $19.95251712 conservative allowance. The LIVE command was not invoked: zero model calls, no comparative scores or genuine comparison replay. See [the funding-gate evidence](docs/v2_1/COMPARISON_FUNDING_BLOCK.md) and [current handoff](docs/v2/CODEX_HANDOFF.md). The preparation and smoke summaries below retain their historical authorization status; this update supersedes those status statements. All frozen artifacts remain unchanged.

**Current development revision: v2.1**, branch `codex/kiodai-v2.1-extraction`. The separately authorized $1 A2 development smoke completed once: 8/8 checkpoints, 3 stored intentions, 7 queries, 3 successful execution receipts, TP3/FP0/FN0 and Set-F1 1.00; 17 calls, API-response cost $0.016737. Sealing initially omitted its dependency and was fully represented only at checkpoint 4. This establishes narrow integration feasibility, not semantic reliability or A2 superiority. Read [the recorded smoke report](docs/v2_1/DEEPSEEK_SMOKE_RESULTS.md) and [current handoff](docs/v2/CODEX_HANDOFF.md). Replay `results/v2_1/deepseek-smoke-v1` with `scripts/serve_v2.py --port 8771 --study results/v2_1/deepseek-smoke-v1`. Read-only verification: `python3 scripts/analyze_v21_smoke.py`. No further paid inference is authorized. All 40 prepared hashes and original run files are preserved.

**Current next step: frozen comparison awaiting authorization.** [Protocol](docs/v2_1/COMPARISON_PROTOCOL.md), [manifest](research/v2_1/comparison_v1.json). All 12 existing development trajectories, A0/B_ledger/A2, one repeat; primary contrast A2 minus B_ledger. Full MOCK verification completed 36 method-trajectories and 288 checkpoints; 162 tests pass. Usage estimate $0.71210, retry-heavy sensitivity $4.57238, conservative allowance $19.95251712; recommended fresh ceiling $20.00. No model inference ran in preparation. Free preflight: `python3 scripts/run_v21_comparison.py --preflight`. The comparison has not been authorized or executed with models; earlier smoke permissions do not carry forward.

The remainder below records historical v2, including its failed smoke and obsolete full-study freeze; its descriptions of “latest” evidence or integration blockers refer to that earlier revision. Current v2.1 needs a separate matching comparative freeze. Historical source verification uses `python3 scripts/verify_saved_smoke.py --historical-sources`.

Working branch: `codex/kiodai-v2`. Submission deadline: **13 September 2026**. Historical A0/A1 files and results remain intact.

The implemented system and B_ledger comparator are in `kiodai_v2/`. Read [method and frozen protocol](docs/v2/METHOD.md), [source-to-design attribution](docs/v2/SOURCES.md), and [manuscript paragraphs and Russian defense](docs/v2/FINDINGS_AND_DEFENSE.md).

**Historical v2 genuine evidence:** the separately authorized $1 A2 smoke completed 8/8 checkpoints but retained no intentions and executed no tasks: TP0/FP0/FN2, Set-F1=0; 24 calls, API-response cost $0.01525668. This is a failed integration result, not a successful memory demonstration. Read [the complete audit and replay instructions](docs/v2/DEEPSEEK_SMOKE_RESULTS.md). Restore with `python3 scripts/restore_deepseek_smoke.py`, then serve `results/v2/deepseek-smoke-v1` on port 8768 for genuine RECORDED output. The archived local/MOCK evidence below remains separate; its claims describe the earlier preparation stage.

For the genuine smoke, restore its archive and verify the existing files without regenerating them:

```bash
python3 scripts/restore_deepseek_smoke.py
python3 scripts/verify_saved_smoke.py
python3 scripts/serve_v2.py --study results/v2/deepseek-smoke-v1 --port 8768
```

Open **http://127.0.0.1:8768/** for genuine RECORDED evidence. The verifier needs no API key or network access. It checks both frozen source inventories, the archive and local file hashes, official scores, response/ledger cost agreement and the absence of a verified-billing claim. It works on a restored copy in another checkout without requiring the unrelated historical run directories. A failed check leaves the evidence unchanged; verification does not repair or replace files.

The original local/MOCK phase has its own archive. Restore it with `python3 scripts/restore_v2_artifacts.py` to inspect unsuccessful development fixtures, the interrupted local model smoke and final software-verification fixtures. Both restore commands refuse to overwrite different local evidence. These are local Git archives, not off-device backups.

```bash
cd /Users/bagnesium/Documents/GitHub/Kiodai
python3 scripts/restore_v2_artifacts.py
python3 scripts/serve_v2.py --study results/v2/mock-verification-v2 --port 8767
```

Open **http://127.0.0.1:8767/** for MOCK software fixtures. Select a trajectory, method and checkpoint. In the `v2_hidden_91320` fixture, checkpoint 1 shows a negative reading and checkpoint 7 a positive reading, selections and execution receipts. These are fixture outcomes; the genuine smoke obtained no readings or receipts. The dashboard reuses the existing stylesheet and keeps evaluator reveal separate. It is read-only; it cannot launch inference. MOCK remains visibly labeled, saved genuine LIVE output is RECORDED, and the local Llama smoke is LOCAL MODEL.

The full original cooling regression can be inspected separately:

```bash
python3 scripts/serve_v2.py --study results/v2/known-cooling-regression-v2 --port 8770
```

Select checkpoints 12 and 13 for the negative and positive sensor-board readings. This is a cooling-only **MOCK** fixture over the entire unchanged original story: TP1/FP0/FN10 overall, including unhandled tasks. Its initial action-label mismatch and unsuccessful trace remain in `results/v2/known-cooling-regression/`. No A0 failure was scripted to manufacture a comparison.

The real local attempt is `results/v2/local-smoke-v2/`: 1/8 completed checkpoints, four model requests, three invalid outputs and one timeout; no usable trajectory. `recovery.json` adds post-hoc finalization while preserving original raw files. View it with `--study results/v2/local-smoke-v2 --port 8769`. The locally started Ollama server was stopped afterward. That local attempt involved no model download or paid API call.

To reproduce software verification in a new output directory:

The full-suite preflight also checks historical A0/A1 preservation, so those saved artifacts must be present for new full-suite MOCK runs. The genuine smoke's read-only verifier above does not require them. Saved-report regeneration commands write derived reports and may update embedded absolute paths on another checkout; use the verifier for a non-mutating evidence check.

```bash
python3 scripts/run_v2.py --mock --output /tmp/kiodai-v2-new-mock
python3 -m unittest discover -s tests -v
python3 scripts/verify_benchmark_integrity.py
scripts/run_smoke_test.sh
python3 -m compileall -q research_harness kiodai_v2 scripts tests
```

The prepared full evaluation is 12 trajectories × A0/B_ledger/A2 × one repeat, with matched full history. Original usage projection: **$0.67219**; revised smoke-informed planning projection: **$1.03943**, with strong representativeness and failed-pipeline caveats. Conservative full allowance remains **$19.95251712**. The original freeze is preserved. The user explicitly deferred this study; the smoke authorization does not cover it. The failed integration is a remaining blocker, and no full evaluation was launched.

The key remains in untracked `.env`; never paste it into chat. Any partial live attempt stops the study and cannot be restarted with a fresh output path or allowance. No additional repetitions or model alternatives are included.

Existing historical replay remains:

```bash
python3 -m research_harness.dashboard --output-root results/followup_v1 --port 8766
```

Choose RECORDED and `live-pair-bdf241965f35`. Original-pilot replay uses `--output-root results/kiodai` and `live-pair-42433e00474b`. Both historical studies found no observed P1 accuracy improvement. The method and resource tables are in the unchanged `FOLLOWUP_RESULTS.md` and `RESULTS.md`.
