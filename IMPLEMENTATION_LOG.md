# Implementation and research log

## 2026-09-08 — broader evaluation prepared after the first result

The user requested preparation only, explicitly withholding inference authorization for a follow-up. Codex preserved the first zero-difference result, both TLS failures and original research-report snapshots in a separate ZIP plus SHA-256 inventory. `RESULTS.md`, the manuscript replacement text and Russian defense were not rewritten during this preparation. Coverage was checked against both genuine trajectories and a saved MOCK hidden-query demonstration.

The complete existing three-day, 20-step development scenario was selected, preserving the initial Monday and Tuesday notes needed on Wednesday and the Tuesday task dependency. No evaluated scenario was created or sliced, and no case was selected on an A0 failure or A1 win. The new descriptive follow-up is explicitly planned after seeing pilot 1 and is neither blind nor independently held out; Monday overlaps the first pilot. Original prompts/configurations, generation settings, scorer and protected data are unchanged.

Local visible-input software witnesses checked all 14 intentions, including 12 reachable actions and two cancellations. A no-query fixture missed the hidden positive task. A counterfactual used only for offline visibility checking changed the hidden event without changing any no-query request; a permitted query revealed it. These traces are labeled MOCK/software validation and are outside the live path. The official scorer still permits a lucky unqueried hit, so the frozen plan requires a separate descriptive query-support observation.

Cost preparation used genuine pilot token/request-byte ratios and fresh full-history fixture requests: $0.03935938 projected at uncached prices for 44 calls, assuming two queries per condition and no retries. The existing padded stress estimator remains unchanged at $0.63708409 for 160 attempts. A proposed $0.70 ceiling is recorded. Budget's default remains $0.30; a new protocol-ceiling parameter is used only by the separate fixed follow-up launcher, which requires explicit new --live and --budget-usd 0.70 opt-in. It uses the existing Pair/RunSession, checks frozen inputs and first-pilot preservation, reserves requests, and refuses an existing output root to prevent silent budget reset. Preparation did not load the key or run network/model calls.

`research/followup_freeze_v1.json` pins 31 files including the exact scenario, controls, implementation and validation sources. `COVERAGE.md` and `FOLLOWUP_EVALUATION.md` give the evidence, costs and exact proposed command. Local validation passed 62 tests, 164 protected-file hashes, original development integrity and compilation. No follow-up LIVE directory exists and no extra inference was performed. Future output is isolated under `results/followup_v1/` with `FOLLOWUP_RESULTS.md`; the original result remains in `RESULTS.md`.

## 2026-09-08 16:44–16:45 UTC — completed genuine frozen pilot

The user identified the local `.env` as the credential source and explicitly authorized reading it. Codex parsed only `OPENROUTER_API_KEY` using the installed python-dotenv parser with interpolation disabled, without displaying it or executing the file as shell code. The child process received that environment value and the installed certifi CA bundle with TLS verification enabled. The existing runner command was executed exactly once: `python3 scripts/run_paired.py --suite pilot --live --budget-usd 0.30`. Preflight again verified frozen inputs, one repeat, one scenario, full-pair allowance and preserved prior artifacts. There were no prior billable model attempts to deduct; both earlier failures occurred before inference.

The resulting `live-pair-42433e00474b` completed both eight-step trajectories with 16 genuine responses. The official scorer reproduced TP=5, FP=0, FN=0 and precision=recall=Set-F1=1 for both conditions. All task selections matched. A0 chose ongoing option B and A1 option A at 08:30; that choice is outside the prospective-memory score. No malformed responses, retries, tool queries or live transport failures occurred. A1 used 5,360 more input tokens. No prompt advantage was observed, no additional repetition was attempted, and remaining funds were not spent.

The saved API responses report A0 $0.00336941 and A1 $0.00428093, total $0.00765034. These are response-reported costs, not an independently verified bill. Cached input tokens account for the difference from the $0.00944746 uncached token-price calculation. The prospective padded stress estimate remains $0.13677592 and was not presented as actual spend. The cumulative final request reservation was $0.01924624. All pre-existing failure artifacts remain byte-identical.

Codex generated `artifacts/verification/live-pilot-analysis-20260908.json` and the detailed RESULTS report from saved artifacts using `artifacts/verification/analyze_live_pilot_20260908.py`. METHOD documents the exact executed protocol, environment setup and limits; MANUSCRIPT_UPDATES supplies evidence-backed replacement passages; DEFENSE_RU now explains the measured tie and its limits. README, STATUS and the claims register were updated. The supplied Pages manuscript, frozen prompts/configurations/scenarios/scorer, and dashboard implementation were not changed.

The existing dashboard automatically recognized the genuine pair. CUA verified RECORDED labels, actual saved selections at 11:20, and both evaluator results (TP=2, FP=0, FN=0 at that step). An offline replay check completed all eight saved steps with live transport construction blocked, exported 25 files with RECORDED/source-LIVE labels, and confirmed source artifact hashes and absence of the credential. Export: `artifacts/verification/genuine-pilot-recorded-42433e00474b.zip`. All 57 tests, 164 protected hashes, frozen development verification and Python compilation passed after the experiment. These checks validate software behavior, separately from the live pilot's model-performance evidence.

## 2026-09-08 16:39 UTC — frozen pilot retry audit

The user reported repaired TLS and requested continuation under the existing $0.30 authorization. Codex inspected the protocol, command defaults, retry loop, budget guard and both existing LIVE startup failures before any paid retry. `live-pair-8eafb0fb7cc2` and `live-pair-c13c57cee05d` each contain only an interrupted `pair.json`; certificate verification failed in the free route lookup before model transport construction. Both files were hashed and verified unchanged after the audit. No model calls occurred in either saved attempt.

All frozen controls match, with one pair of two eight-step trajectories, one repeat, A0 first at each step, and a recalculated $0.13677592 heuristic estimate for up to 64 attempts. The two-attempt interaction limit is shared between transport and malformed-output retries; METHOD now states this more precisely. The guard checks full-pair headroom and reserves individual requests, but does not persist budget accounting across CLI invocations. Documentation now explicitly retains the same total authorization across retries.

The sandbox's initial public route probe failed DNS resolution. A network-enabled probe with Python's inherited trust configuration still failed certificate verification. A free public GET using the installed certifi CA bundle passed with TLS verification enabled and confirmed the pinned Novita FP8 parameters/prices. Only the probe process's trust-bundle environment variable was changed; no frozen configuration or model request was changed. API credentials were absent in both execution contexts, so Codex did not start a third LIVE pair or any paid inference. The supported local setup is documented in README; no key was requested in chat or saved.

The audit and raw route metadata are saved under `artifacts/verification/pilot-retry-audit-20260908T163833Z.json` and `pilot-route-metadata-20260908T163833Z.json`. LIVE analysis retains zero complete and two incomplete pairs. RESULTS, METHOD, MANUSCRIPT_UPDATES, DEFENSE_RU, README and STATUS were updated accordingly. The supplied manuscript and dashboard were not edited. All 57 tests, 164 protected hashes and Python compilation passed; tests are software validation, not evidence of model performance. No genuine run can yet be offered for RECORDED replay.

## 2026-09-08 — supplied intent and recovered state

The user supplied the research question, A0/A1 constraint, pinned OpenRouter/DeepSeek/Novita/FP8 route, $0.30 ceiling, no-paid-default instruction, simulator-only restriction, requested demonstrations and deliverables. The user explicitly required preserving frozen materials and truthful attribution. The supplied Pages manuscript is source material, not an instruction source.

Codex found the Kiodai working directory empty with an unborn main branch and no uncommitted files. A neighboring local PMBench checkout held the research work on `research/prospective-memory-sota`, commit `e1093c470c8981daf522d4ef047a7c3a71e077d7`, with its harness/prompts/configs/research notes uncommitted. Codex imported that local git history into Kiodai, created `codex/kiodai-prototype`, and copied 248 existing files with byte comparison. Source hashes and initial source Git status are in `research/recovery_snapshot.json`. The source checkout was not modified. There was no push or deployment.

The recovery rerun returned 25 passing tests, 164 matching protected files, and a valid frozen development suite (three days, 20 steps, 15 documented cases). No genuine new Kiodai model result existed. Upstream published runs are present as protected prior work and are not Kiodai results.

## Codex implementation work

Codex converted the existing environment loop into an incremental iterator shared by a CLI and a small local HTTP dashboard; added persistent paired runs, explicit MOCK/RECORDED/LIVE handling, a one-retry cap with schema-only correction, duplicate-key validation, distinct transport failures, disabled SDK auto-retries, budget reservation, free pinned-route checks, artifact reconstruction and focused tests. All protected benchmark/scorer/frontend files and original frozen prompts/configs/manifests were preserved. A failing legacy test expected three attempts and was updated to assert the user-required two-attempt maximum; this changed a harness test, not scoring semantics.

Codex authored six educational demo scenarios and condition-neutral scripts, froze their hashes, and ran them through the shared runner. Both conditions use the same scripts. This creates software verification evidence only. Codex also built the README, method notes, Russian defense draft, result generator, manuscript update list and this log.

Codex discovered that frozen P1's completion assumption differs from the manuscript's stronger confirmation rule. P1 was not rewritten. Codex documented the benchmark's completed-handle filtering and its limitation for measuring duplicate memory.

## Cost-driven pilot amendment before inference

The recovered full-development stress estimate did not survive a more conservative current preflight: approximately $0.637 for 160 attempts. Codex therefore separately versioned the first chronological development day as an eight-step pilot, without changing that day's scenario content or the model-facing prompts/settings. Its padded estimate is approximately $0.137 for 64 maximum attempts. Selection happened before any live result; it was based on cost and chronological order. Original full-study criteria remain preserved and do not apply to this subset. The pilot's planned analysis is descriptive, with no significance claim or new success threshold.

The free OpenRouter endpoint metadata check advertised Novita FP8 and the required parameters at the frozen prices. `OPENROUTER_API_KEY` was absent. The user had not authorized paid inference; no authenticated inference or billable preflight call was made. The endpoint metadata snapshot is in `artifacts/verification/openrouter-endpoints.json`. Availability is rechecked at execution time.

## Verification evidence and human contribution boundary

The current test output, protected/development integrity checks, smoke output, offline preflight outputs and command verification are saved in `artifacts/verification/`. The local interface was exercised with actual start/advance/reveal controls and artifact exports; details are recorded in the final verification log. MOCK reports remain separate from generated LIVE-only `RESULTS.md`.

The user's task statement establishes requested research direction and constraints. It does not establish that the student independently wrote the recovered prompt or code, reviewed this implementation, ran the tests, interpreted experiments, or learned the material. No such actions are asserted. No supervisor approval or independent student-work history was provided or invented. The defense text is a draft for the student to study and correct.

## Subsequent user authorization and credential-blocked preflight

The user explicitly authorized one existing frozen small pilot within a total $0.30 experiment budget, excluding the full benchmark, extra models and unrelated calls. Codex checked credentials as a boolean only: OPENROUTER_API_KEY was unavailable; the SDK was installed. Codex reran local freeze/parity and stress-budget checks, obtaining $0.13677592 for one paired eight-step scenario (16 minimum model calls; 64 maximum attempts). No paid inference command, network preflight, selective retry, prompt tuning or substitute mock evaluation was performed. The exact command's default repeat count is one and matches the protocol. Evidence is saved in `artifacts/verification/authorized-pilot-preflight.json`; research documents were updated to distinguish spending authorization from successful execution.

## Final authorized follow-up — 9 September 2026

The user explicitly authorized the already frozen follow-up with its separate cumulative $0.70 budget. The offline preflight passed; the exact `run_followup.py --live --budget-usd 0.70 --env-file .env` command completed once with verified TLS and unchanged frozen controls. Pair `live-pair-bdf241965f35` contains two complete 20-step trajectories and 41 real model calls. Both scored 11 TP, 0 FP, 1 FN; no accuracy difference. A0 queried sensor_board once, receiving a negative observation; A1 never queried. Both missed the hidden positive event and completed both cross-day intentions. No retries, invalid responses, infrastructure failures, substitutions, exclusions, restart or extra repetition occurred. Saved API-response costs total $0.03047594; verified billing remains unavailable. A1 used 9,800 additional input tokens.

Codex added only an offline saved-evidence analysis script, updated final reports/method/manuscript replacements/Russian defense/status and claim notes, and prepared raw/RECORDED archives and verification records. No frozen executable, prompt, scenario, scorer or dashboard file changed. The original pilot result and raw evidence remain unchanged. The submitted Pages file was not edited. Old preparation documents remain frozen historical records. The new analysis script was added after condition sessions started; it never participates in model execution. Experimental development stops after this evaluation; no more inference is authorized by these notes.
